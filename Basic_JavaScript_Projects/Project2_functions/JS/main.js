function start() { // Creating function
    var Alpha = "Hello, "; // Setting variable
    Alpha += "human."; // Concatenating strings
    document.getElementById("text").innerHTML = Alpha; // Printing Alpha to the element with the ID "text"
}